services:
  app:
    build: .
    ports:
      - "8002:8002"
    env_file: .env
    volumes:
      - ./src:/app/src
      - ./data:/app/data
    command: uvicorn src.main:app --host 0.0.0.0 --port 8002 --reload
